const conf = {
  "SiteName": "TacAM-Login",

  "signup": {
    "PwRegex": "^(?=.*[a-z])(?=.*[A-Z])(?=.*[0-9])(?=.*[!@#$%^&*])(?=.{8,})"
  },
  //Backend-Server for Login/Signup Handling
  "backend": {
    "login" : "https://domain.tld/login",
    "signup": "https://domain.tld/signup"
  }
}